name: 'Build'

on:
  push:
    branches:
      - master
      - dev
      - liteelement
  pull_request:
    branches:
      - master
      - dev
      - liteelement

jobs:
  build:
    name: Test build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build
        run: |
          npm install
          npm run build
